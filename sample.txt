#!/bin/bash
# Stop any old processes
fuser -k 6666/tcp 2>/dev/null
# Launch in background and force it to keep running
nohup dufs /workspaces/codespace-storage -p 6666 --allow-all > dufs.log 2>&1 &
echo "Dufs is running in the background. Check dufs.log for details."